apiVersion: v1
kind: Service
metadata:
  name: hangang
  labels:
    app.kubernetes.io/name: hangang
spec:
  selector:
    app.kubernetes.io/name: hangang
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
