# Example schema only. Do not apply this placeholder or commit a real token.
# Use the private-file kubectl command in docs/KUBERNETES.md instead.
apiVersion: v1
kind: Secret
metadata:
  name: hangang-admin
stringData:
  token: "" # Must be supplied privately; an empty token fails gateway startup.
